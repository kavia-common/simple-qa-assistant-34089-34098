# Development-only settings for CRA dev server
# Fixes "Invalid Host header" when accessing the dev server via a non-localhost hostname (e.g. preview URLs).
# See: https://create-react-app.dev/docs/advanced-configuration/
DANGEROUSLY_DISABLE_HOST_CHECK=true
